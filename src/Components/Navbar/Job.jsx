import React from 'react';

const Job = ({job}) => {
    const { company_name, description, jobTitle, location, logo, salary } = job;
    console.log(job);
    return (
        <div>
            
        </div>
    );
};

export default Job;